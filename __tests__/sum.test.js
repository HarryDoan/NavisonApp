import {beforeEach, describe, expect, test} from '@jest/globals';

describe('sum module', () => {
  beforeEach(() => {
    // initializeCityDatabase();
  });

  afterEach(() => {
    // clearCityDatabase();
  });
});
