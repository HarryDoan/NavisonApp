import {beforeEach, describe, expect, test} from '@jest/globals';
import {sum} from '@screens/common/Sum';

describe('sum module', () => {
  beforeEach(() => {
    // initializeCityDatabase();
  });

  afterEach(() => {
    // clearCityDatabase();
  });
});
