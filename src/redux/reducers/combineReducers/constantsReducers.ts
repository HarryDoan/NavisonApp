import actions from '@redux/actions';
import {reducerDefault} from '@redux/common/reducers';

// export const getConfigApp = (...props) => {
//   return reducerDefault(...props, actions.GET_CONFIG_APP);
// };
